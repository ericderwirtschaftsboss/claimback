# Database
DATABASE_URL="postgresql://claimback:claimback_dev@localhost:5432/claimback"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"

# Google OAuth (used for sign-in + Gmail access)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Anthropic (Claude AI)
ANTHROPIC_API_KEY=""

# Encryption key for storing OAuth tokens (32 bytes hex)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=""

# Demo mode - set to "true" to use fixtures instead of real APIs
DEMO_MODE="true"
